.navbar
  .navbar-inner
    .container{style: 'width: auto;'}
      = link_to 'Studio K', '#', class: 'brand'

      %ul.nav.pull-right
        - if user_signed_in?
          %li= link_to 'Logout', destroy_user_session_path, :method => :delete
        - else
          %li= link_to 'Login', new_user_session_path
          %li= link_to 'Register', new_user_registration_path
